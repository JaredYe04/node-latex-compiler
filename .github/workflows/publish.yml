name: Publish to npm

on:
  release:
    types: [created]
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to publish (patch, minor, major)'
        required: true
        default: 'patch'
        type: choice
        options:
          - patch
          - minor
          - major

jobs:
  prepare:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.version-dispatch.outputs.version || steps.version-release.outputs.version }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: true

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          registry-url: 'https://registry.npmjs.org'

      - name: Configure Git
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"

      - name: Sync with remote (workflow_dispatch)
        if: github.event_name == 'workflow_dispatch'
        run: |
          echo "🔄 Fetching latest changes from remote..."
          DEFAULT_BRANCH=$(git remote show origin | grep "HEAD branch" | cut -d" " -f5 || echo "")
          if [ -z "$DEFAULT_BRANCH" ]; then
            for branch in main master; do
              if git ls-remote --heads origin $branch | grep -q .; then
                DEFAULT_BRANCH=$branch
                break
              fi
            done
          fi
          if [ -z "$DEFAULT_BRANCH" ]; then
            DEFAULT_BRANCH="main"
          fi
          echo "🔄 Using branch: $DEFAULT_BRANCH"
          git fetch origin $DEFAULT_BRANCH || true
          CURRENT_BRANCH=$(git branch --show-current 2>/dev/null || echo "")
          if [ -z "$CURRENT_BRANCH" ] || [ "$CURRENT_BRANCH" != "$DEFAULT_BRANCH" ]; then
            git checkout $DEFAULT_BRANCH || true
          fi
          git pull origin $DEFAULT_BRANCH --rebase || git pull origin $DEFAULT_BRANCH || true

      - name: Install dependencies
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          if [ -f package-lock.json ]; then
            npm ci
          else
            npm install
          fi

      - name: Bump version (workflow_dispatch)
        if: github.event_name == 'workflow_dispatch'
        id: version-dispatch
        shell: bash
        run: |
          LOCAL_VERSION=$(node -p "require('./package.json').version")
          echo "📦 Local package.json version: $LOCAL_VERSION"

          NPM_VERSION=$(npm view node-latex-compiler version 2>/dev/null || echo "")

          if [ -z "$NPM_VERSION" ]; then
            echo "📦 No existing version on npm, starting fresh"
            BASE_VERSION=$LOCAL_VERSION
          else
            echo "📦 Latest npm version: $NPM_VERSION"

            COMPARE=$(node -e "
              const local = '$LOCAL_VERSION'.split('.').map(Number);
              const npm = '$NPM_VERSION'.split('.').map(Number);
              for (let i = 0; i < 3; i++) {
                if (local[i] > npm[i]) {
                  console.log('local');
                  process.exit(0);
                } else if (local[i] < npm[i]) {
                  console.log('npm');
                  process.exit(0);
                }
              }
              console.log('equal');
            ")

            if [ "$COMPARE" = "npm" ] || [ "$COMPARE" = "equal" ]; then
              echo "⚠️  npm version ($NPM_VERSION) is newer or equal to local ($LOCAL_VERSION)"
              npm version $NPM_VERSION --no-git-tag-version --allow-same-version
              BASE_VERSION=$NPM_VERSION
            else
              echo "✅ Local version ($LOCAL_VERSION) is newer than npm ($NPM_VERSION)"
              BASE_VERSION=$LOCAL_VERSION
            fi
          fi

          CURRENT=$(node -p "require('./package.json').version")
          if [ "$CURRENT" != "$BASE_VERSION" ]; then
            npm version $BASE_VERSION --no-git-tag-version --allow-same-version
          fi

          echo "🆕 Incrementing version (${{ github.event.inputs.version }})..."
          npm version ${{ github.event.inputs.version }} --no-git-tag-version
          NEW_VERSION=$(node -p "require('./package.json').version")
          echo "🆕 New version: $NEW_VERSION"

          if npm view node-latex-compiler@$NEW_VERSION version > /dev/null 2>&1; then
            echo "⚠️  Version $NEW_VERSION already exists, incrementing again..."
            npm version ${{ github.event.inputs.version }} --no-git-tag-version
            NEW_VERSION=$(node -p "require('./package.json').version")

            if npm view node-latex-compiler@$NEW_VERSION version > /dev/null 2>&1; then
              echo "❌ Error: Version $NEW_VERSION also exists on npm"
              exit 1
            fi
          fi

          echo "version=$NEW_VERSION" >> $GITHUB_OUTPUT
          echo "VERSION=$NEW_VERSION" >> $GITHUB_ENV

      - name: Bump version (release)
        if: github.event_name == 'release'
        id: version-release
        shell: bash
        run: |
          VERSION=${GITHUB_REF#refs/tags/v}
          echo "Release version: $VERSION"

          if npm view node-latex-compiler@$VERSION version > /dev/null 2>&1; then
            echo "⚠️  Warning: Version $VERSION already exists on npm"
          fi

          npm version $VERSION --no-git-tag-version --allow-same-version
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "VERSION=$VERSION" >> $GITHUB_ENV
          echo "✅ Version set: $VERSION"

      - name: Verify version
        run: |
          VERSION=$(node -p "require('./package.json').version")
          echo "📦 Version to publish: $VERSION"
          echo "✅ Version ready"

  build-and-publish-runtimes:
    needs: prepare
    strategy:
      matrix:
        include:
          - platform: linux
            arch: x64
            runner: ubuntu-latest
            package: '@node-latex-compiler/bin-linux-x64'
          - platform: win32
            arch: x64
            runner: windows-latest
            package: '@node-latex-compiler/bin-win32-x64'
          - platform: darwin
            arch: x64
            runner: macos-14
            package: '@node-latex-compiler/bin-darwin-x64'
          - platform: darwin
            arch: arm64
            runner: macos-14
            package: '@node-latex-compiler/bin-darwin-arm64'
    runs-on: ${{ matrix.runner }}
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          registry-url: 'https://registry.npmjs.org'

      - name: Install dependencies
        shell: bash
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          FORCE_PLATFORM: ${{ matrix.platform }}
          FORCE_ARCH: ${{ matrix.arch }}
        run: |
          if [ -f package-lock.json ]; then
            npm ci
          else
            npm install
          fi

      - name: Download Tectonic binary
        shell: bash
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          FORCE_PLATFORM: ${{ matrix.platform }}
          FORCE_ARCH: ${{ matrix.arch }}
        run: |
          echo "📥 Downloading Tectonic binary for ${{ matrix.platform }} ${{ matrix.arch }}..."
          node scripts/download-tectonic.js || {
            echo "❌ Binary download failed"
            exit 1
          }

      - name: Verify binary exists
        shell: bash
        run: |
          if [ "${{ matrix.platform }}" = "win32" ]; then
            BINARY="bin/${{ matrix.platform }}-${{ matrix.arch }}/tectonic.exe"
          else
            BINARY="bin/${{ matrix.platform }}-${{ matrix.arch }}/tectonic"
          fi
          
          if [ ! -f "$BINARY" ]; then
            echo "❌ Error: Binary not found at $BINARY"
            exit 1
          fi
          
          echo "✓ Binary found at $BINARY"
          
          # Verify binary works
          # Note: Tectonic doesn't support --version, so we use --help instead
          if [ "${{ matrix.platform }}" = "win32" ]; then
            "$BINARY" --help > /dev/null 2>&1 || {
              echo "❌ Binary verification failed"
              exit 1
            }
          else
            chmod +x "$BINARY"
            "$BINARY" --help > /dev/null 2>&1 || {
              echo "❌ Binary verification failed"
              exit 1
            }
          fi
          
          echo "✅ Binary verification passed"

      - name: Run compilation tests
        shell: bash
        run: |
          echo "🧪 Running compilation tests..."
          node scripts/test-compilation.js || {
            echo "❌ Tests failed"
            exit 1
          }
          echo "✅ All tests passed"

      - name: Create runtime directory
        shell: bash
        run: |
          mkdir -p runtimes/@node-latex-compiler/bin-${{ matrix.platform }}-${{ matrix.arch }}/bin

      - name: Copy binary to runtime package
        shell: bash
        run: |
          if [ "${{ matrix.platform }}" = "win32" ]; then
            SOURCE="bin/${{ matrix.platform }}-${{ matrix.arch }}/tectonic.exe"
          else
            SOURCE="bin/${{ matrix.platform }}-${{ matrix.arch }}/tectonic"
          fi
          TARGET="runtimes/@node-latex-compiler/bin-${{ matrix.platform }}-${{ matrix.arch }}/bin"
          
          cp "$SOURCE" "$TARGET/" || {
            echo "❌ Failed to copy binary"
            exit 1
          }
          
          if [ "${{ matrix.platform }}" != "win32" ]; then
            chmod +x "$TARGET/tectonic"
          fi
          
          echo "✅ Binary copied to runtime package"

      - name: Create package.json for runtime
        shell: bash
        run: |
          echo "📝 Creating package.json for ${{ matrix.package }}..."
          node scripts/create-runtime-package-json.js ${{ matrix.platform }} ${{ matrix.arch }} ${{ needs.prepare.outputs.version }}

      - name: Verify NPM authentication
        shell: bash
        run: |
          echo "🔍 Verifying npm authentication..."
          npm whoami || {
            echo "❌ Error: Not authenticated to npm"
            exit 1
          }
          echo "✅ Successfully authenticated to npm"
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Check npm organization exists
        shell: bash
        run: |
          echo "🔍 Checking if npm organization @node-latex-compiler exists..."
          
          ERROR_OUTPUT=$(npm view @node-latex-compiler/bin-linux-x64@999.0.0 2>&1 || true)
          
          if echo "$ERROR_OUTPUT" | grep -q "Scope not found"; then
            echo ""
            echo "❌ ERROR: npm organization @node-latex-compiler does not exist!"
            echo ""
            echo "📝 You must create the npm organization before publishing runtime packages."
            echo ""
            echo "   Quick setup:"
            echo "   1. Go to: https://www.npmjs.com/org/create"
            echo "   2. Create organization: node-latex-compiler"
            echo "   3. Choose 'Public' (free for public packages)"
            echo ""
            exit 1
          elif echo "$ERROR_OUTPUT" | grep -q "not in this registry\|404"; then
            echo "✅ npm organization @node-latex-compiler exists"
          else
            echo "⚠️  Could not definitively verify organization, but continuing..."
          fi
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        continue-on-error: false

      - name: Check if runtime package version exists
        id: check-version
        shell: bash
        run: |
          VERSION="${{ needs.prepare.outputs.version }}"
          if npm view ${{ matrix.package }}@$VERSION version > /dev/null 2>&1; then
            echo "⚠️  Version $VERSION already exists for ${{ matrix.package }}"
            echo "exists=true" >> $GITHUB_OUTPUT
          else
            echo "✅ Version $VERSION is available for ${{ matrix.package }}"
            echo "exists=false" >> $GITHUB_OUTPUT
          fi
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        continue-on-error: true

      - name: Publish runtime package
        if: steps.check-version.outputs.exists != 'true'
        shell: bash
        run: |
          echo "📦 Publishing ${{ matrix.package }}@${{ needs.prepare.outputs.version }}..."
          cd runtimes/@node-latex-compiler/bin-${{ matrix.platform }}-${{ matrix.arch }}
          npm publish --access public
          echo "✅ Published ${{ matrix.package }}@${{ needs.prepare.outputs.version }}"
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Publish runtime package (skip if exists)
        if: steps.check-version.outputs.exists == 'true'
        shell: bash
        run: |
          echo "ℹ️  Version ${{ needs.prepare.outputs.version }} already exists for ${{ matrix.package }}, skipping publish"

  publish-main:
    needs: [prepare, build-and-publish-runtimes]
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: true
          ref: ${{ github.event.release.tag_name || github.ref }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          registry-url: 'https://registry.npmjs.org'

      - name: Configure Git
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"

      - name: Install dependencies
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          if [ -f package-lock.json ]; then
            npm ci
          else
            npm install
          fi

      - name: Update version and optionalDependencies
        run: |
          VERSION="${{ needs.prepare.outputs.version }}"
          echo "📦 Updating main package to version $VERSION"
          
          # Update version
          npm version $VERSION --no-git-tag-version --allow-same-version
          
          # Update optionalDependencies to match runtime package versions
          node -e "
            const fs = require('fs');
            const pkg = JSON.parse(fs.readFileSync('package.json', 'utf8'));
            const version = '$VERSION';
            
            pkg.optionalDependencies = {
              '@node-latex-compiler/bin-win32-x64': '^' + version,
              '@node-latex-compiler/bin-darwin-x64': '^' + version,
              '@node-latex-compiler/bin-darwin-arm64': '^' + version,
              '@node-latex-compiler/bin-linux-x64': '^' + version
            };
            
            fs.writeFileSync('package.json', JSON.stringify(pkg, null, 2) + '\n');
            console.log('✅ Updated optionalDependencies to version ' + version);
          "

      - name: Verify NPM authentication
        run: |
          echo "🔍 Verifying npm authentication..."
          npm whoami || {
            echo "❌ Error: Not authenticated to npm"
            exit 1
          }
          echo "✅ Successfully authenticated to npm"
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Verify version before publish
        run: |
          VERSION="${{ needs.prepare.outputs.version }}"
          echo "📦 Preparing to publish main package version: $VERSION"

          if npm view node-latex-compiler@$VERSION version > /dev/null 2>&1; then
            echo "❌ Error: Version $VERSION already exists on npm registry"
            exit 1
          fi

          echo "✅ Version $VERSION is ready to publish"
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Publish main package
        run: |
          echo "📦 Publishing main package to npm..."
          npm publish --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        continue-on-error: false

      - name: Create Git tag and push (workflow_dispatch)
        if: github.event_name == 'workflow_dispatch'
        run: |
          VERSION="${{ needs.prepare.outputs.version }}"
          
          # Add package.json and lock files if they exist
          git add package.json
          [ -f package-lock.json ] && git add package-lock.json || true
          [ -f yarn.lock ] && git add yarn.lock || true
          [ -f pnpm-lock.yaml ] && git add pnpm-lock.yaml || true

          if git diff --staged --quiet; then
            echo "⚠️  No changes to commit"
          else
            git commit -m "chore: bump version to $VERSION" || echo "Nothing to commit"
          fi

          echo "🔄 Pulling latest changes from remote..."
          DEFAULT_BRANCH=$(git remote show origin | grep "HEAD branch" | cut -d" " -f5 || echo "")
          if [ -z "$DEFAULT_BRANCH" ]; then
            for branch in main master; do
              if git ls-remote --heads origin $branch | grep -q .; then
                DEFAULT_BRANCH=$branch
                break
              fi
            done
          fi
          if [ -z "$DEFAULT_BRANCH" ]; then
            DEFAULT_BRANCH="main"
          fi
          echo "🔄 Using branch: $DEFAULT_BRANCH"
          git fetch origin $DEFAULT_BRANCH || true
          git pull origin $DEFAULT_BRANCH --rebase || git pull origin $DEFAULT_BRANCH --no-edit || {
            echo "⚠️  Warning: Failed to sync with remote, but continuing..."
          }

          if git rev-parse "v$VERSION" >/dev/null 2>&1; then
            echo "⚠️  Tag v$VERSION already exists locally"
          else
            git tag -a "v$VERSION" -m "Release v$VERSION"
          fi

          echo "📤 Pushing changes to remote..."
          git push origin HEAD:$DEFAULT_BRANCH || {
            echo "❌ Failed to push to branch: $DEFAULT_BRANCH"
            exit 1
          }

          if git ls-remote --tags origin | grep -q "refs/tags/v$VERSION$"; then
            echo "ℹ️  Tag v$VERSION already exists on remote"
          else
            git push origin "v$VERSION" || {
              echo "⚠️  Failed to push tag, but code was pushed successfully"
            }
          fi

          echo "✅ Successfully pushed version $VERSION"

      - name: Create GitHub Release (workflow_dispatch)
        if: github.event_name == 'workflow_dispatch'
        uses: softprops/action-gh-release@v1
        with:
          tag_name: v${{ needs.prepare.outputs.version }}
          name: Release v${{ needs.prepare.outputs.version }}
          body: |
            ## Changes in v${{ needs.prepare.outputs.version }}

            See [CHANGELOG.md](CHANGELOG.md) for details.

            ### ✨ Features
            - Automatic binary download for your platform
            - Multiple input/output formats (file, text, buffer)
            - Full stdout/stderr streaming support
            - Platform-specific binary packages

